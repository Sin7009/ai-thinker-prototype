# AI Thinker: The "Homo Cogitans" Architecture

![Python](https://img.shields.io/badge/Python-3.10-blue?style=for-the-badge&logo=python) ![LangChain](https://img.shields.io/badge/LangChain-Core-green?style=for-the-badge) ![Architecture](https://img.shields.io/badge/Architecture-Hybrid%20Mind-orange?style=for-the-badge) ![Docker](https://img.shields.io/badge/Docker-Ready-blue?style=for-the-badge)

> **"Мы не автоматизируем ответы. Мы аугментируем мышление."**

**AI Thinker** — это прототип когнитивного агента нового поколения. В отличие от стандартных LLM-оберток (RAG/Chatbots), архитектура этого проекта построена на фундаменте **отечественной психологической школы (Выготский, Лурия, Анохин)**.

Это не просто "помощник". Это **Методологический Партнер**, который диагностирует ваши когнитивные искажения и помогает "думать об него", используя научные протоколы мышления.

---

## 🧠 Философия Архитектуры: "Гибридный Разум"

Мы решаем проблему "Парадокса продуктивности ИИ": когда доступ к GPT делает сотрудника быстрее, но глупее. Наша система реализует три принципа:

1.  **Зона Ближайшего Развития (Л.С. Выготский):**
    Агент не дает готовых решений в сложных задачах. Он использует **Scaffolding** (строительные леса) — задает вопросы, структурирует хаос, заставляя пользователя самого прийти к инсайту.
2.  **Нейропсихологическая Диагностика (А.Р. Лурия):**
    Мы анализируем не только *что* сказано, но и *как*. Фоновый контур постоянно сканирует речь на предмет когнитивных искажений (катастрофизация, черно-белое мышление) и эмоционального фона.
3.  **Акцептор Результата Действия (П.К. Анохин):**
    Система памяти не просто хранит логи. Она строит динамическую модель пользователя ("Theory of Mind"), предсказывая, какая методология сработает лучше всего в данный момент.

---

## 🏗 Архитектура Системы (Visual Flow)

Система реализует принцип **Двухконтурного управления**: детерминированный код управляет вероятностными моделями (LLM).

```mermaid
flowchart TB
    %% Узлы и связи
    User(["Пользователь"]) --> Input["Ввод текста"]
    Input --> Orch{{"Orchestrator"}}
    
    %% Контур Б
    Orch -. Параллельный анализ .-> Detector["Detector Agent"]
    Detector -. Когнитивные искажения & особенности личности .-> Orch
    
    %% Выбор режима
    Orch --> CheckMode{"Автоматический выбор режима"}
    
    %% Ветвь Copilot
    CheckMode -- Copilot --> Task["Task Agent"]
    Task -- Прямой ответ --> Output(["Вывод"])
    
    %% Ветвь Partner (исправленная логика выбора техники)
    CheckMode -- Partner --> Diagnose["Диагностика проблемы"]
    Diagnose -- "Запрос на выбор техники" --> Task
    Task -- "Вызов функции" --> Method["Methodology Agent"]
    
    %% Результат работы методолога
    Method -- Скэффолдинг / Вопросы --> Output
    
    %% Память и замыкание круга
    Output --> Memory[("Dynamic Memory")]
    Memory <-. RAG & Profile .-> Orch
    Output --> User

    %% --- Стилизация ---
    classDef core fill:#2d3436,stroke:#fff,stroke-width:3px,color:#fff
    classDef agent fill:#0984e3,stroke:#fff,stroke-width:1px,color:#fff
    classDef memory fill:#6c5ce7,stroke:#fff,stroke-width:1px,color:#fff
    classDef decision fill:#fab1a0,stroke:#333,stroke-width:1px,color:#000
    
    class Orch core
    class Detector,Task,Method agent
    class Memory memory
    class CheckMode,Diagnose decision
````

-----

## ⚙️ Спецификация модулей (System Design)

### ⬛️ 1. Orchestrator (Оркестратор)

**Тип:** Управляющий модуль (State Machine)
**Файл:** `orchestrator/orchestrator.py`

  * **Функция:** Центральный узел принятия решений. Он не генерирует текст ответа, а управляет потоками данных: запускает параллельный анализ, определяет режим работы (Copilot/Partner) и маршрутизирует задачи между агентами.
  * **Методология:** **«Лобные доли»**. Реализация функции когнитивного контроля и торможения. Мы заменяем вероятностную природу LLM на детерминированный код (Python) в вопросах соблюдения правил и процедур.
  * **Промпт:** *Отсутствует (Это алгоритмический код на Python).*

### 🟦 2. Detector Agent (Детектор)

**Тип:** Фоновый аналитик
**Файл:** `agents/detector_agent.py`

  * **Функция:** Работает параллельно с вводом. Не участвует в диалоге напрямую. Анализирует *как* говорит пользователь: выявляет структурные нарушения мышления (когнитивные искажения), уровень аффекта и стиль коммуникации.
  * **Методология:** **Нейропсихологическая диагностика (А.Р. Лурия)**. Реконструкция состояния высших психических функций (ВПФ) через анализ речи.
  * **System Prompt:**
    ```text
    Ты — Нейропсихолог-диагност и психолингвист, работающий по методологии А.Р. Лурии.
    Твоя задача — по речи реконструировать состояние высших психических функций (ВПФ) пользователя.
    1. Анализ мышления (Патопсихология): Ищи не просто "ошибки", а структурные нарушения...
    2. Анализ аффекта (Выготский): Если видишь сильный аффект, отметь: "Когнитивная способность снижена"...
    3. Речевой профиль: Ригидный, Импульсивный или Рефлексивный?
    Верни результат СТРОГО в формате JSON.
    ```

### 🟧 3. Диагностика & Task Agent (Универсальный Агент)

**Тип:** Исполнитель / Диагност
**Файл:** `agents/task_agent.py`

  * **Функция (Двойное назначение):**
    1.  **В режиме Copilot:** Дает прямой ответ, пишет код, ищет информацию.
    2.  **В режиме Partner:** Получает "Запрос на выбор техники" от блока Диагностики. Анализирует проблему и выбирает лучший инструмент из библиотеки (например, "Тут нужен 'Метод Утенка'").
  * **Методология:** **ReAct (Reasoning + Acting) + Деятельностный подход (Леонтьев)**. Различение «Операции» (автоматическое действие) и «Деятельности» (осознанный процесс с мотивом).
  * **System Prompt:**
    ```text
    Ты — Когнитивный Оператор (CodeName: Vygotsky-1).
    Твоя архитектура основана на деятельностном подходе А.Н. Леонтьева.
    1. Различай Операцию и Мотив: Пользователь присылает 'Операцию'. Реконструируй 'Мотив'.
    2. Принцип Доминанты (Ухтомский): Определи текущую доминанту.
    3. Зона ближайшего развития: Не делай за пользователя то, что он может сделать сам.
    ```

### 🟦 4. Methodology Agent (Когнитивный Тренер)

**Тип:** Исполнитель протоколов
**Файлы:** `agents/methodology_agent.py` + `orchestrator/action_library.py`

  * **Функция:** Реализация выбранной техники мышления. Агент **не дает ответов**, а ведет пользователя через жесткий сценарий (скрипт), который был выбран Task Agent-ом.
  * **Методология:** **Скэффолдинг (Scaffolding / Выготский)**. Создание «строительных лесов» для мышления. Агент берет на себя функцию организации процесса, оставляя пользователю функцию содержания.
  * **System Prompt (Пример: "Метод Утенка"):**
    ```text
    Твоя роль — фасилитатор этапа 'Громкой речи' (по Гальперину).
    Пользователь находится на этапе материализации действия. Ему нужно проговорить проблему вслух...
    Твоя задача: Слушать и возвращать ему его же тезисы, но структурированными.
    Не давай решений! Дай ему завершить интериоризацию самостоятельно.
    ```

### 🟪 5. Dynamic Memory (Динамическая Память)

**Тип:** Гибридное хранилище
**Файл:** `orchestrator/dynamic_memory.py`

  * **Функция:** Обеспечивает связность контекста (RAG) и персонализацию (Profile). Принимает вывод диалога и сохраняет значимые факты.
  * **Методология:** **Акцептор Результата Действия (П.К. Анохин)**. Память формирует предсказательную модель того, что пользователю может понадобиться в будущем, основываясь на прошлом опыте.
  * **Структура:**
      * *SQLite:* Профиль личности, счетчики паттернов, факты.
      * *ChromaDB:* Векторные эмбеддинги диалогов для семантического поиска.

-----

## 🛠 Технический стек

  * **Core:** Python 3.10+
  * **Orchestration:** LangChain (Core/Community)
  * **LLM Backend:** OpenRouter (поддержка Google Gemini 2.0, DeepSeek R1, Grok) — *легко переключается в конфиге*.
  * **Memory:**
      * **Vector:** ChromaDB (локальное хранение эмбеддингов).
      * **Relational:** SQLAlchemy (SQLite по умолчанию, PostgreSQL ready).
  * **Interface:** Telegram Bot API (асинхронный).
  * **Infrastructure:** Docker & Docker Compose.

-----

## 🚀 Быстрый старт

### Вариант A: Docker (Рекомендуется)

1.  **Клонируйте репозиторий:**

    ```bash
    git clone [https://github.com/your-username/ai-thinker-prototype.git](https://github.com/your-username/ai-thinker-prototype.git)
    cd ai-thinker-prototype
    ```

2.  **Настройте окружение:**
    Создайте файл `.env` в корне проекта:

    ```bash
    OPENROUTER_API_KEY=sk-or-v1-ваш_ключ
    TELEGRAM_TOKEN=ваш_токен_бота
    ```

3.  **Запустите:**

    ```bash
    docker-compose up --build -d
    ```

### Вариант B: Локальный запуск (Dev)

1.  Установите зависимости:
    ```bash
    pip install -r requirements.txt
    ```
2.  Запустите бота:
    ```bash
    python telegram_bot.py
    ```
    *При первом запуске автоматически скачаются модели для эмбеддингов и создастся база данных.*

-----

## 🎮 Режимы использования

В чате с ботом доступны команды:

  * `/copilot` — **Режим Ассистента**. Быстрые ответы, факты, код. (Использует быструю модель).
  * `/partner` — **Режим Мыслителя**. Бот переходит на "умную" модель (DeepSeek-R1 / Gemini 2.0), перестает давать советы и начинает "распаковывать" вашу проблему вопросами.
  * `/memory` — **Зеркало**. Бот расскажет, что он понял о вас: ваши паттерны, стиль общения и темы.
  * `/exit` — **Завершение сессии**. Бот проанализирует диалог, сохранит инсайты в долговременную память и выдаст резюме.

-----

*Project by Konstantin Knyazev. 2025.*
*Лицензия: MIT.*

```
```
